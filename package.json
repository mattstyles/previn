{
  "name": "previn",
  "version": "0.0.0",
  "description": "Composes classes together to create inheritance chains, enabling a sort of multiple inheritance",
  "main": "lib/index.js",
  "scripts": {
    "prepack": "npm run build",
    "build": "babel src --out-dir lib",
    "watch": "babel src --out-dir lib --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Matt Styles",
  "repository": "git://github.com/mattstyles/previn.git",
  "license": "WTFPL",
  "dependencies": {
  },
  "devDependencies": {
    "babel": "5.8.23",
    "babel-eslint": "^4.1.1",
    "eslint": "^1.3.1"
  }
}
